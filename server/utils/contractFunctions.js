const mintEpisodeToken = (tokenURI) => {

    // After minted needs to let the stra know that there is a token in the pool addExternalErc721Award(address _externalErc721, uint256[] calldata _tokenIds)
}

const getCurrentToken = () => {
    // getTokenCount returns total number of tokens
    // pass in the count to tokenURI function to get ipfs string
    // get the name from the returned json 
    // compare against the name value for the most recent axios request
}

const startWinnerSelection = () => {

}

const startPrizeContest = () => {

}